<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book-Notes</title>
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <div class="box">
        <div class="addNewBook">
            <h1 class="NewHeading">Add A New Book</h1>
            <div class="bookForm">
                <form class="add-form" action="/add" method="post">
                    <input class="title" type="text" name="newTitle" placeholder="Title">
                    <textarea class="desc" name="newDescription" placeholder="Description" autocomplete="off"></textarea>
                    <input class="isbn" type="text" name="newIsbn" placeholder="ISBN">
                    <input class="rating" type="text" name="newRating" placeholder="Rating">
                    <button class="submit" type="submit" name="list">Submit</button>
                </form>
            </div>
        </div>

        <div class="container">
            <h1 class="heading">List of Books</h1>
            <ol>
                <% for(let book of bookItems){ %>
                    <div class="abook">
                        <div class="book-image">
                            <img class="cover" src="https://covers.openlibrary.org/b/isbn/<%=book.isbn%>-M.jpg" alt="Book Cover"/>
                        </div>
                        <div class="book-info">
                            <h2><%= book.title %></h2>
                            <p class="book-description"><%= book.description %></p>
                            <p class="book-rating">Rating: <%= book.rating %></p>

                            <form action="/delete" method="post" class="delete-form">
                                <input type="hidden" name="isbn" value="<%= book.isbn %>">
                                <button class="submit" type="submit" name="delete">Delete</button>
                            </form>
                            <form action="/edit" method="get" class="delete-form">
                                <input type="hidden" name="isbn" value="<%= book.isbn %>">
                                <button class="submit" type="submit" name="edit">Edit</button>
                            </form>
                        </div>
                    </div>
                <% } %>
            </ol>
        </div>
    </div>
</body>
</html>